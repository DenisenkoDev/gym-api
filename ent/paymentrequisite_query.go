// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"gym-api/ent/gym"
	"gym-api/ent/paymentrequisite"
	"gym-api/ent/predicate"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PaymentRequisiteQuery is the builder for querying PaymentRequisite entities.
type PaymentRequisiteQuery struct {
	config
	ctx        *QueryContext
	order      []paymentrequisite.OrderOption
	inters     []Interceptor
	predicates []predicate.PaymentRequisite
	withGym    *GymQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PaymentRequisiteQuery builder.
func (prq *PaymentRequisiteQuery) Where(ps ...predicate.PaymentRequisite) *PaymentRequisiteQuery {
	prq.predicates = append(prq.predicates, ps...)
	return prq
}

// Limit the number of records to be returned by this query.
func (prq *PaymentRequisiteQuery) Limit(limit int) *PaymentRequisiteQuery {
	prq.ctx.Limit = &limit
	return prq
}

// Offset to start from.
func (prq *PaymentRequisiteQuery) Offset(offset int) *PaymentRequisiteQuery {
	prq.ctx.Offset = &offset
	return prq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (prq *PaymentRequisiteQuery) Unique(unique bool) *PaymentRequisiteQuery {
	prq.ctx.Unique = &unique
	return prq
}

// Order specifies how the records should be ordered.
func (prq *PaymentRequisiteQuery) Order(o ...paymentrequisite.OrderOption) *PaymentRequisiteQuery {
	prq.order = append(prq.order, o...)
	return prq
}

// QueryGym chains the current query on the "gym" edge.
func (prq *PaymentRequisiteQuery) QueryGym() *GymQuery {
	query := (&GymClient{config: prq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := prq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := prq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(paymentrequisite.Table, paymentrequisite.FieldID, selector),
			sqlgraph.To(gym.Table, gym.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, paymentrequisite.GymTable, paymentrequisite.GymColumn),
		)
		fromU = sqlgraph.SetNeighbors(prq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PaymentRequisite entity from the query.
// Returns a *NotFoundError when no PaymentRequisite was found.
func (prq *PaymentRequisiteQuery) First(ctx context.Context) (*PaymentRequisite, error) {
	nodes, err := prq.Limit(1).All(setContextOp(ctx, prq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{paymentrequisite.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (prq *PaymentRequisiteQuery) FirstX(ctx context.Context) *PaymentRequisite {
	node, err := prq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PaymentRequisite ID from the query.
// Returns a *NotFoundError when no PaymentRequisite ID was found.
func (prq *PaymentRequisiteQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = prq.Limit(1).IDs(setContextOp(ctx, prq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{paymentrequisite.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (prq *PaymentRequisiteQuery) FirstIDX(ctx context.Context) int {
	id, err := prq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PaymentRequisite entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PaymentRequisite entity is found.
// Returns a *NotFoundError when no PaymentRequisite entities are found.
func (prq *PaymentRequisiteQuery) Only(ctx context.Context) (*PaymentRequisite, error) {
	nodes, err := prq.Limit(2).All(setContextOp(ctx, prq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{paymentrequisite.Label}
	default:
		return nil, &NotSingularError{paymentrequisite.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (prq *PaymentRequisiteQuery) OnlyX(ctx context.Context) *PaymentRequisite {
	node, err := prq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PaymentRequisite ID in the query.
// Returns a *NotSingularError when more than one PaymentRequisite ID is found.
// Returns a *NotFoundError when no entities are found.
func (prq *PaymentRequisiteQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = prq.Limit(2).IDs(setContextOp(ctx, prq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{paymentrequisite.Label}
	default:
		err = &NotSingularError{paymentrequisite.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (prq *PaymentRequisiteQuery) OnlyIDX(ctx context.Context) int {
	id, err := prq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PaymentRequisites.
func (prq *PaymentRequisiteQuery) All(ctx context.Context) ([]*PaymentRequisite, error) {
	ctx = setContextOp(ctx, prq.ctx, ent.OpQueryAll)
	if err := prq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PaymentRequisite, *PaymentRequisiteQuery]()
	return withInterceptors[[]*PaymentRequisite](ctx, prq, qr, prq.inters)
}

// AllX is like All, but panics if an error occurs.
func (prq *PaymentRequisiteQuery) AllX(ctx context.Context) []*PaymentRequisite {
	nodes, err := prq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PaymentRequisite IDs.
func (prq *PaymentRequisiteQuery) IDs(ctx context.Context) (ids []int, err error) {
	if prq.ctx.Unique == nil && prq.path != nil {
		prq.Unique(true)
	}
	ctx = setContextOp(ctx, prq.ctx, ent.OpQueryIDs)
	if err = prq.Select(paymentrequisite.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (prq *PaymentRequisiteQuery) IDsX(ctx context.Context) []int {
	ids, err := prq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (prq *PaymentRequisiteQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, prq.ctx, ent.OpQueryCount)
	if err := prq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, prq, querierCount[*PaymentRequisiteQuery](), prq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (prq *PaymentRequisiteQuery) CountX(ctx context.Context) int {
	count, err := prq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (prq *PaymentRequisiteQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, prq.ctx, ent.OpQueryExist)
	switch _, err := prq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (prq *PaymentRequisiteQuery) ExistX(ctx context.Context) bool {
	exist, err := prq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PaymentRequisiteQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (prq *PaymentRequisiteQuery) Clone() *PaymentRequisiteQuery {
	if prq == nil {
		return nil
	}
	return &PaymentRequisiteQuery{
		config:     prq.config,
		ctx:        prq.ctx.Clone(),
		order:      append([]paymentrequisite.OrderOption{}, prq.order...),
		inters:     append([]Interceptor{}, prq.inters...),
		predicates: append([]predicate.PaymentRequisite{}, prq.predicates...),
		withGym:    prq.withGym.Clone(),
		// clone intermediate query.
		sql:  prq.sql.Clone(),
		path: prq.path,
	}
}

// WithGym tells the query-builder to eager-load the nodes that are connected to
// the "gym" edge. The optional arguments are used to configure the query builder of the edge.
func (prq *PaymentRequisiteQuery) WithGym(opts ...func(*GymQuery)) *PaymentRequisiteQuery {
	query := (&GymClient{config: prq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	prq.withGym = query
	return prq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BankKard1 string `json:"bank_kard_1,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PaymentRequisite.Query().
//		GroupBy(paymentrequisite.FieldBankKard1).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (prq *PaymentRequisiteQuery) GroupBy(field string, fields ...string) *PaymentRequisiteGroupBy {
	prq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PaymentRequisiteGroupBy{build: prq}
	grbuild.flds = &prq.ctx.Fields
	grbuild.label = paymentrequisite.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BankKard1 string `json:"bank_kard_1,omitempty"`
//	}
//
//	client.PaymentRequisite.Query().
//		Select(paymentrequisite.FieldBankKard1).
//		Scan(ctx, &v)
func (prq *PaymentRequisiteQuery) Select(fields ...string) *PaymentRequisiteSelect {
	prq.ctx.Fields = append(prq.ctx.Fields, fields...)
	sbuild := &PaymentRequisiteSelect{PaymentRequisiteQuery: prq}
	sbuild.label = paymentrequisite.Label
	sbuild.flds, sbuild.scan = &prq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PaymentRequisiteSelect configured with the given aggregations.
func (prq *PaymentRequisiteQuery) Aggregate(fns ...AggregateFunc) *PaymentRequisiteSelect {
	return prq.Select().Aggregate(fns...)
}

func (prq *PaymentRequisiteQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range prq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, prq); err != nil {
				return err
			}
		}
	}
	for _, f := range prq.ctx.Fields {
		if !paymentrequisite.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if prq.path != nil {
		prev, err := prq.path(ctx)
		if err != nil {
			return err
		}
		prq.sql = prev
	}
	return nil
}

func (prq *PaymentRequisiteQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PaymentRequisite, error) {
	var (
		nodes       = []*PaymentRequisite{}
		_spec       = prq.querySpec()
		loadedTypes = [1]bool{
			prq.withGym != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PaymentRequisite).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PaymentRequisite{config: prq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, prq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := prq.withGym; query != nil {
		if err := prq.loadGym(ctx, query, nodes, nil,
			func(n *PaymentRequisite, e *Gym) { n.Edges.Gym = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (prq *PaymentRequisiteQuery) loadGym(ctx context.Context, query *GymQuery, nodes []*PaymentRequisite, init func(*PaymentRequisite), assign func(*PaymentRequisite, *Gym)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*PaymentRequisite)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	query.withFKs = true
	query.Where(predicate.Gym(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(paymentrequisite.GymColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.payment_requisite_gym
		if fk == nil {
			return fmt.Errorf(`foreign-key "payment_requisite_gym" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "payment_requisite_gym" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (prq *PaymentRequisiteQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := prq.querySpec()
	_spec.Node.Columns = prq.ctx.Fields
	if len(prq.ctx.Fields) > 0 {
		_spec.Unique = prq.ctx.Unique != nil && *prq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, prq.driver, _spec)
}

func (prq *PaymentRequisiteQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(paymentrequisite.Table, paymentrequisite.Columns, sqlgraph.NewFieldSpec(paymentrequisite.FieldID, field.TypeInt))
	_spec.From = prq.sql
	if unique := prq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if prq.path != nil {
		_spec.Unique = true
	}
	if fields := prq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, paymentrequisite.FieldID)
		for i := range fields {
			if fields[i] != paymentrequisite.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := prq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := prq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := prq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := prq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (prq *PaymentRequisiteQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(prq.driver.Dialect())
	t1 := builder.Table(paymentrequisite.Table)
	columns := prq.ctx.Fields
	if len(columns) == 0 {
		columns = paymentrequisite.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if prq.sql != nil {
		selector = prq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if prq.ctx.Unique != nil && *prq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range prq.predicates {
		p(selector)
	}
	for _, p := range prq.order {
		p(selector)
	}
	if offset := prq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := prq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PaymentRequisiteGroupBy is the group-by builder for PaymentRequisite entities.
type PaymentRequisiteGroupBy struct {
	selector
	build *PaymentRequisiteQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (prgb *PaymentRequisiteGroupBy) Aggregate(fns ...AggregateFunc) *PaymentRequisiteGroupBy {
	prgb.fns = append(prgb.fns, fns...)
	return prgb
}

// Scan applies the selector query and scans the result into the given value.
func (prgb *PaymentRequisiteGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, prgb.build.ctx, ent.OpQueryGroupBy)
	if err := prgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PaymentRequisiteQuery, *PaymentRequisiteGroupBy](ctx, prgb.build, prgb, prgb.build.inters, v)
}

func (prgb *PaymentRequisiteGroupBy) sqlScan(ctx context.Context, root *PaymentRequisiteQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(prgb.fns))
	for _, fn := range prgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*prgb.flds)+len(prgb.fns))
		for _, f := range *prgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*prgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := prgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PaymentRequisiteSelect is the builder for selecting fields of PaymentRequisite entities.
type PaymentRequisiteSelect struct {
	*PaymentRequisiteQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (prs *PaymentRequisiteSelect) Aggregate(fns ...AggregateFunc) *PaymentRequisiteSelect {
	prs.fns = append(prs.fns, fns...)
	return prs
}

// Scan applies the selector query and scans the result into the given value.
func (prs *PaymentRequisiteSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, prs.ctx, ent.OpQuerySelect)
	if err := prs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PaymentRequisiteQuery, *PaymentRequisiteSelect](ctx, prs.PaymentRequisiteQuery, prs, prs.inters, v)
}

func (prs *PaymentRequisiteSelect) sqlScan(ctx context.Context, root *PaymentRequisiteQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(prs.fns))
	for _, fn := range prs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*prs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := prs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
