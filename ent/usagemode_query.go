// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"gym-api/ent/predicate"
	"gym-api/ent/usagemode"
	"gym-api/ent/user"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UsageModeQuery is the builder for querying UsageMode entities.
type UsageModeQuery struct {
	config
	ctx        *QueryContext
	order      []usagemode.OrderOption
	inters     []Interceptor
	predicates []predicate.UsageMode
	withUser   *UserQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UsageModeQuery builder.
func (umq *UsageModeQuery) Where(ps ...predicate.UsageMode) *UsageModeQuery {
	umq.predicates = append(umq.predicates, ps...)
	return umq
}

// Limit the number of records to be returned by this query.
func (umq *UsageModeQuery) Limit(limit int) *UsageModeQuery {
	umq.ctx.Limit = &limit
	return umq
}

// Offset to start from.
func (umq *UsageModeQuery) Offset(offset int) *UsageModeQuery {
	umq.ctx.Offset = &offset
	return umq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (umq *UsageModeQuery) Unique(unique bool) *UsageModeQuery {
	umq.ctx.Unique = &unique
	return umq
}

// Order specifies how the records should be ordered.
func (umq *UsageModeQuery) Order(o ...usagemode.OrderOption) *UsageModeQuery {
	umq.order = append(umq.order, o...)
	return umq
}

// QueryUser chains the current query on the "user" edge.
func (umq *UsageModeQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: umq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := umq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := umq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(usagemode.Table, usagemode.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, usagemode.UserTable, usagemode.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(umq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first UsageMode entity from the query.
// Returns a *NotFoundError when no UsageMode was found.
func (umq *UsageModeQuery) First(ctx context.Context) (*UsageMode, error) {
	nodes, err := umq.Limit(1).All(setContextOp(ctx, umq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{usagemode.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (umq *UsageModeQuery) FirstX(ctx context.Context) *UsageMode {
	node, err := umq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UsageMode ID from the query.
// Returns a *NotFoundError when no UsageMode ID was found.
func (umq *UsageModeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = umq.Limit(1).IDs(setContextOp(ctx, umq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{usagemode.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (umq *UsageModeQuery) FirstIDX(ctx context.Context) int {
	id, err := umq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UsageMode entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UsageMode entity is found.
// Returns a *NotFoundError when no UsageMode entities are found.
func (umq *UsageModeQuery) Only(ctx context.Context) (*UsageMode, error) {
	nodes, err := umq.Limit(2).All(setContextOp(ctx, umq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{usagemode.Label}
	default:
		return nil, &NotSingularError{usagemode.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (umq *UsageModeQuery) OnlyX(ctx context.Context) *UsageMode {
	node, err := umq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UsageMode ID in the query.
// Returns a *NotSingularError when more than one UsageMode ID is found.
// Returns a *NotFoundError when no entities are found.
func (umq *UsageModeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = umq.Limit(2).IDs(setContextOp(ctx, umq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{usagemode.Label}
	default:
		err = &NotSingularError{usagemode.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (umq *UsageModeQuery) OnlyIDX(ctx context.Context) int {
	id, err := umq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UsageModes.
func (umq *UsageModeQuery) All(ctx context.Context) ([]*UsageMode, error) {
	ctx = setContextOp(ctx, umq.ctx, ent.OpQueryAll)
	if err := umq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UsageMode, *UsageModeQuery]()
	return withInterceptors[[]*UsageMode](ctx, umq, qr, umq.inters)
}

// AllX is like All, but panics if an error occurs.
func (umq *UsageModeQuery) AllX(ctx context.Context) []*UsageMode {
	nodes, err := umq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UsageMode IDs.
func (umq *UsageModeQuery) IDs(ctx context.Context) (ids []int, err error) {
	if umq.ctx.Unique == nil && umq.path != nil {
		umq.Unique(true)
	}
	ctx = setContextOp(ctx, umq.ctx, ent.OpQueryIDs)
	if err = umq.Select(usagemode.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (umq *UsageModeQuery) IDsX(ctx context.Context) []int {
	ids, err := umq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (umq *UsageModeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, umq.ctx, ent.OpQueryCount)
	if err := umq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, umq, querierCount[*UsageModeQuery](), umq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (umq *UsageModeQuery) CountX(ctx context.Context) int {
	count, err := umq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (umq *UsageModeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, umq.ctx, ent.OpQueryExist)
	switch _, err := umq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (umq *UsageModeQuery) ExistX(ctx context.Context) bool {
	exist, err := umq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UsageModeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (umq *UsageModeQuery) Clone() *UsageModeQuery {
	if umq == nil {
		return nil
	}
	return &UsageModeQuery{
		config:     umq.config,
		ctx:        umq.ctx.Clone(),
		order:      append([]usagemode.OrderOption{}, umq.order...),
		inters:     append([]Interceptor{}, umq.inters...),
		predicates: append([]predicate.UsageMode{}, umq.predicates...),
		withUser:   umq.withUser.Clone(),
		// clone intermediate query.
		sql:  umq.sql.Clone(),
		path: umq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (umq *UsageModeQuery) WithUser(opts ...func(*UserQuery)) *UsageModeQuery {
	query := (&UserClient{config: umq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	umq.withUser = query
	return umq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Mode usagemode.Mode `json:"mode,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UsageMode.Query().
//		GroupBy(usagemode.FieldMode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (umq *UsageModeQuery) GroupBy(field string, fields ...string) *UsageModeGroupBy {
	umq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UsageModeGroupBy{build: umq}
	grbuild.flds = &umq.ctx.Fields
	grbuild.label = usagemode.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Mode usagemode.Mode `json:"mode,omitempty"`
//	}
//
//	client.UsageMode.Query().
//		Select(usagemode.FieldMode).
//		Scan(ctx, &v)
func (umq *UsageModeQuery) Select(fields ...string) *UsageModeSelect {
	umq.ctx.Fields = append(umq.ctx.Fields, fields...)
	sbuild := &UsageModeSelect{UsageModeQuery: umq}
	sbuild.label = usagemode.Label
	sbuild.flds, sbuild.scan = &umq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UsageModeSelect configured with the given aggregations.
func (umq *UsageModeQuery) Aggregate(fns ...AggregateFunc) *UsageModeSelect {
	return umq.Select().Aggregate(fns...)
}

func (umq *UsageModeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range umq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, umq); err != nil {
				return err
			}
		}
	}
	for _, f := range umq.ctx.Fields {
		if !usagemode.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if umq.path != nil {
		prev, err := umq.path(ctx)
		if err != nil {
			return err
		}
		umq.sql = prev
	}
	return nil
}

func (umq *UsageModeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UsageMode, error) {
	var (
		nodes       = []*UsageMode{}
		_spec       = umq.querySpec()
		loadedTypes = [1]bool{
			umq.withUser != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UsageMode).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UsageMode{config: umq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, umq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := umq.withUser; query != nil {
		if err := umq.loadUser(ctx, query, nodes, nil,
			func(n *UsageMode, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (umq *UsageModeQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*UsageMode, init func(*UsageMode), assign func(*UsageMode, *User)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*UsageMode)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	query.withFKs = true
	query.Where(predicate.User(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(usagemode.UserColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.usage_mode_user
		if fk == nil {
			return fmt.Errorf(`foreign-key "usage_mode_user" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "usage_mode_user" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (umq *UsageModeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := umq.querySpec()
	_spec.Node.Columns = umq.ctx.Fields
	if len(umq.ctx.Fields) > 0 {
		_spec.Unique = umq.ctx.Unique != nil && *umq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, umq.driver, _spec)
}

func (umq *UsageModeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(usagemode.Table, usagemode.Columns, sqlgraph.NewFieldSpec(usagemode.FieldID, field.TypeInt))
	_spec.From = umq.sql
	if unique := umq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if umq.path != nil {
		_spec.Unique = true
	}
	if fields := umq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, usagemode.FieldID)
		for i := range fields {
			if fields[i] != usagemode.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := umq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := umq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := umq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := umq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (umq *UsageModeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(umq.driver.Dialect())
	t1 := builder.Table(usagemode.Table)
	columns := umq.ctx.Fields
	if len(columns) == 0 {
		columns = usagemode.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if umq.sql != nil {
		selector = umq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if umq.ctx.Unique != nil && *umq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range umq.predicates {
		p(selector)
	}
	for _, p := range umq.order {
		p(selector)
	}
	if offset := umq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := umq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UsageModeGroupBy is the group-by builder for UsageMode entities.
type UsageModeGroupBy struct {
	selector
	build *UsageModeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (umgb *UsageModeGroupBy) Aggregate(fns ...AggregateFunc) *UsageModeGroupBy {
	umgb.fns = append(umgb.fns, fns...)
	return umgb
}

// Scan applies the selector query and scans the result into the given value.
func (umgb *UsageModeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, umgb.build.ctx, ent.OpQueryGroupBy)
	if err := umgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UsageModeQuery, *UsageModeGroupBy](ctx, umgb.build, umgb, umgb.build.inters, v)
}

func (umgb *UsageModeGroupBy) sqlScan(ctx context.Context, root *UsageModeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(umgb.fns))
	for _, fn := range umgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*umgb.flds)+len(umgb.fns))
		for _, f := range *umgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*umgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := umgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UsageModeSelect is the builder for selecting fields of UsageMode entities.
type UsageModeSelect struct {
	*UsageModeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ums *UsageModeSelect) Aggregate(fns ...AggregateFunc) *UsageModeSelect {
	ums.fns = append(ums.fns, fns...)
	return ums
}

// Scan applies the selector query and scans the result into the given value.
func (ums *UsageModeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ums.ctx, ent.OpQuerySelect)
	if err := ums.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UsageModeQuery, *UsageModeSelect](ctx, ums.UsageModeQuery, ums, ums.inters, v)
}

func (ums *UsageModeSelect) sqlScan(ctx context.Context, root *UsageModeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ums.fns))
	for _, fn := range ums.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ums.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ums.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
